[package]
name = "duet"
description = "Duet binary: wiring, CLI, single-instance, lifecycle."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
duet-types = { path = "../duet-types" }
# T-4.1.1: the bootstrap itself -- see duet-ui's crate doc for why this
# dependency is safe under ADR-002 (duet-ui owns gpui, not this crate).
duet-ui = { path = "../duet-ui" }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
