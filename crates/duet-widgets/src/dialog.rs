// SPDX-License-Identifier: MIT
//! Modal dialog façade over `gpui_component::dialog` (R-G7): confirmation
//! prompts, overwrite/rename dialogs, progress dialogs, etc. Use
//! `duet_widgets::dialog::*` instead of `gpui_component::dialog` directly.

pub use gpui_component::dialog::*;
