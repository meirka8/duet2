[package]
name = "duet-meta"
description = "MIME detection, icon lookup, thumbnails, desktop entries, associations."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
# T-5.3.4 launching: `setsid` for the detached child (same workspace pin
# duet-vfs uses; the `process` feature is already on).
rustix.workspace = true

[dev-dependencies]
tempfile = "3"
